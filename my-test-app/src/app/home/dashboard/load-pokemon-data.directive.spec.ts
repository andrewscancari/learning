import { LoadPokemonDataDirective } from './load-pokemon-data.directive';

describe('LoadPokemonDataDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadPokemonDataDirective();
    expect(directive).toBeTruthy();
  });
});
